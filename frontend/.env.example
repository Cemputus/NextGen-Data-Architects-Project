# Backend URL. In development, app uses http://127.0.0.1:5000 by default (no proxy).
# Set to empty string to use the dev-server proxy instead: REACT_APP_API_URL=
# REACT_APP_API_URL=http://127.0.0.1:5000
