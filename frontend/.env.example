# Backend URL. Set so all API calls (including User Management) hit the Flask server.
# Without this, the dev proxy may not forward correctly and you can get 404.
REACT_APP_API_URL=http://localhost:5000
