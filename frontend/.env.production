# Production: Vercel SPA → Render API (HTTPS). Keeps JWT session traffic aligned with backend CORS.
REACT_APP_API_URL=https://nextgen-mis.onrender.com
# Session behaviour enabled unless set to 1 (omit in production).
# REACT_APP_DISABLE_SESSION_EXPIRY=1
DISABLE_ESLINT_PLUGIN=true
